export interface IdResults {
    // For functions where you'll need the ID of the resource generated
    id: string;
    results: Results;
}

export interface Results {
    // Standard console output for EVERY request 
    message: string;
    status: number;
    //...etc
}

export interface ErrorResults {





    
}

import { Billing } from "../src";
import { assert } from "chai";


export function makePrimary(id: string) {
    if (!id){ 
        throw new Error("Please insert an existing billing ID");
    }


};

describe("Tests Billing", async () => {

});
